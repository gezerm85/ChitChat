import { StyleSheet } from "react-native";
import { color, bgColor2 } from "../../utils/Colors";

export default styles = StyleSheet.create({
  container: {
    backgroundColor: bgColor2,
    borderRadius: 50,
    width: 60,
    height: 60,
    justifyContent: "center",
    alignItems: "center",
    borderColor: color,
  },
});
